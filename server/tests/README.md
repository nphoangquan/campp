# Server Tests

Thư mục này chứa toàn bộ test dùng `Jest` cho backend `server/`.

Phân chia:

- `setup/`: file khởi tạo môi trường test
- `helpers/`: helper dùng lại cho test
- `unit/`: unit test cho validator, permission, utils
- `integration/`: integration test theo từng module nghiệp vụ
